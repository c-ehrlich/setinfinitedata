demo for trpc setInfiniteData issue

- rename .env-example to .env
- docker-compose up to start db
- pnpm dev to start app
- create a post and look at the console, posts will be optimistically updated to `{foo: "bar"}`